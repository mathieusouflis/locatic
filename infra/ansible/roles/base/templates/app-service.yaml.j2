apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  namespace: {{ app_namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ app_name }}
  ports:
    - port: {{ app_port }}
      targetPort: {{ app_port }}
